'use client'

import { useState } from "react";

interface Props {
    pagina: number;
}

export default function DigimonPages({}:Props) {

    const [pagina, setPagina] = useState(1);

    return (
        <div className="">
 
        </div>
    );
}